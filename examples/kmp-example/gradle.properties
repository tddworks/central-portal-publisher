# Project information
GROUP=com.tddworks.example
VERSION_NAME=1.0.0

# Sonatype credentials (set these in ~/.gradle/gradle.properties)
#SONATYPE_USERNAME=your-username
#SONATYPE_PASSWORD=your-token

# Signing (set these in ~/.gradle/gradle.properties)
#SIGNING_KEY=-----BEGIN PGP PRIVATE KEY BLOCK-----...
#SIGNING_PASSWORD=your-gpg-password

# Build optimization
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true